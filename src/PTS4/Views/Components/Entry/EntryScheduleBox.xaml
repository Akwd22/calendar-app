<?xml version="1.0" encoding="UTF-8"?>
<ContentView xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PTS4.Views.Components.EntryParts.EntryScheduleBox">
    <ContentView.Resources>
        <Style TargetType="Label">
            <Setter Property="TextColor" Value="#707070" />
            <Setter Property="FontSize" Value="12" />
        </Style>
    </ContentView.Resources>
    <ContentView.Content>
        <Frame BackgroundColor="#D9FFFFFF" CornerRadius="10" HeightRequest="50" WidthRequest="60" Padding="0">
            <StackLayout HorizontalOptions="Center" VerticalOptions="Center">
                <Label Text="{Binding StartTime, StringFormat='{0:HH:mm}'}" />
                <Label Text="{Binding EndTime, StringFormat='{0:HH:mm}'}" />
            </StackLayout>
        </Frame>
    </ContentView.Content>
</ContentView>